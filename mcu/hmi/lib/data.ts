// Data layer for manual control
// Uses the API client to interact with the backend

import apiClient from './api-client';

// Export the API client for direct use in manual control
export default apiClient; 